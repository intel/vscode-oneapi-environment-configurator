{
	"name": "oneapi-environment-variables",
	"displayName": "Environment Configurator for Intel oneAPI Toolkits",
	"description": "Configure environment for working with Intel oneAPI Toolkit",
	"version": "1.0.1",
	"license": "MIT",
	"icon": "media/oneapi-logo.png",
	"publisher": "intel-corporation",
	"engines": {
		"vscode": "^1.46.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": [
			{
				"title": "Environment Configurator for Intel oneAPI Toolkits",
				"properties": {
					"intel-corporation.oneapi-environment-variables.ONEAPI_ROOT": {
						"type": "string",
						"default": [],
						"description": "Absolute path to oneAPI installation folder.\n\nSpecifies the absolute pathname to your oneAPI installation. This path is useful if the oneAPI environment extension is unable to automatically locate your oneAPI setvars script. This is most likely to happen if you have customized the location of your oneAPI install. Note that, by default, this setting only applies if the automatic search for the oneAPI setvars script fails.\n\n"
					},
					"intel-corporation.oneapi-environment-variables.SETVARS_CONFIG": {
						"type": "array",
						"default": [],
						"description": "Name of a custom setvars configuration file.\n\nSpecify the name of a setvars configuration file. The oneAPI setvars environment setup script normally initializes the 'latest' version of each installed oneAPI component (tools and libraries). This default behavior can be overridden by way of a 'setvars config file'. If no pathname is specified (only a filename is provided) the config file is assumed to be located in the ${workspaceFolder}/.vscode/ directory; otherwise, if a pathname+filename is provided the config file will be located as named.\nRead more about this feature for [Linux and macOS](https://software.intel.com/) and for [Windows](https://software.intel.com/content/www/us/en/develop/documentation/oneapi-programming-guide/).\n",
						"items": {
							"type": "string"
						},
						"scope": "resource"
					}
				}
			}
		],
		"commands": [
			{
				"command": "intel-corporation.oneapi-environment-variables.initializeEnvironment",
				"title": "Intel oneAPI: Initialize environment variables"
			},
			{
				"command": "intel-corporation.oneapi-environment-variables.initializeEnvironmentConfig",
				"title": "Intel oneAPI: Initialize custom environment variables using SETVARS_CONFIG"
			},
			{
				"command": "intel-corporation.oneapi-environment-variables.clearEnvironment",
				"title": "Intel oneAPI: Clear environment variables"
			},
			{
				"command": "intel-corporation.oneapi-environment-variables.switchEnv",
				"title": "Intel oneAPI: Switch environment"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"package": "vsce package --yarn",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "./node_modules/.bin/extest setup-and-run -y -o ./src/test/ui/test-configs/settings.json -m ./src/test/ui/test-configs/.mocharc.js ./out/test/ui/ui.test.js"
	},
	"devDependencies": {
		"@types/chai": "^4.2.15",
		"@types/glob": "^7.1.3",
		"@types/mocha": "^8.0.0",
		"@types/node": "^12.11.7",
		"@types/vscode": "^1.46.0",
		"@typescript-eslint/eslint-plugin": "^4.1.1",
		"@typescript-eslint/parser": "^4.1.1",
		"chai": "^4.3.0",
		"eslint": "^7.9.0",
		"glob": "^7.1.6",
		"mocha": "^8.1.3",
		"typescript": "^4.0.2",
		"vsce": "^1.81.1",
		"vscode-extension-tester": "^4.0.3",
		"vscode-extension-tester-native": "^3.0.2",
		"vscode-test": "^1.4.0"
	},
	"dependencies": {},
	"repository": {
		"type": "git",
		"url": "https://github.com/intel/vscode-environment-and-launch-configurator.git"
	}
}
